{
  "name": "urformance",
  "version": "1.0.0",
  "private": true,
  "description": "Urformance monorepo — frontend + backend",
  "scripts": {
    "dev:backend": "npm --prefix backend run dev",
    "start:backend": "npm --prefix backend start",
    "install:backend": "npm --prefix backend install"
  },
  "workspaces": [
    "backend"
  ]
}
